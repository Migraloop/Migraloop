[package]
name = "migraloop-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared TLS, Managed-field mapping, column shape, secret-reference, and Output Identity key types"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
