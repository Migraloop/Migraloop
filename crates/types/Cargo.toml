[package]
name = "migraloop-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared TLS, Managed-field mapping, and secret-reference types for the apply path"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
